import { FC } from "react";
import { BiMenu } from "react-icons/bi";

const Menu: FC = () => {
	return (
		<div className="menu flex-h">
			<h2>menu</h2>
			<BiMenu />
		</div>
	);
};

export default Menu;
